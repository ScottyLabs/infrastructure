diff --git a/pkg/bridgeidentity/map.go b/pkg/bridgeidentity/map.go
--- a/pkg/bridgeidentity/map.go
+++ b/pkg/bridgeidentity/map.go
@@ -10,6 +10,7 @@ import (
 	"regexp"
 	"strings"
 	"sync"
+	"time"
 
 	"maunium.net/go/mautrix/id"
 )
@@ -33,8 +34,10 @@ type Map struct {
 }
 
 var (
-	loadOnce          sync.Once
+	mapMu             sync.RWMutex
 	globalMap         *Map
+	loadedPath        string
+	loadedMod         time.Time
 	discordGhostRegex = regexp.MustCompile(`^@discord_([0-9]+):`)
 )
 
@@ -47,10 +50,28 @@ func DefaultPath() string {
 
 // Get returns the loaded identity map (empty if missing or invalid).
 func Get() *Map {
-	loadOnce.Do(func() {
-		globalMap = load(DefaultPath())
-	})
-	return globalMap
+	path := DefaultPath()
+	mapMu.RLock()
+	if globalMap != nil && loadedPath == path {
+		if info, err := os.Stat(path); err == nil && !info.ModTime().After(loadedMod) {
+			m := globalMap
+			mapMu.RUnlock()
+			return m
+		}
+	}
+	mapMu.RUnlock()
+
+	mapMu.Lock()
+	defer mapMu.Unlock()
+	globalMap = load(path)
+	loadedPath = path
+	if info, err := os.Stat(path); err == nil {
+		loadedMod = info.ModTime()
+	} else {
+		loadedMod = time.Time{}
+	}
+	return globalMap
 }
 
 func load(path string) *Map {
