apiVersion: 1
policies:
  - orgId: 1
    receiver: discord
    group_by: [alertname, instance, monitor_name]
    group_wait: 30s
    group_interval: 5m
    repeat_interval: 4h
    routes:
      - receiver: slack
        continue: true
